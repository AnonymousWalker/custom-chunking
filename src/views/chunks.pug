extends index.pug

block action
    +action Create Chunks

block breadcrumbs
    -
        var crumbs = [
            {name: "Home", href: "/"},
            {name: params.lang, href: `/${params.lang}`},
            {name: params.res, href: `/${params.lang}/${params.res}`},
            {name: params.book, href: `/${params.lang}/${params.res}/${params.book}`}
        ]
    +breadcrumbs(crumbs, params.chapter)
block content
    ul(class='menu')
        each chunk in chunks
            li(class="chunk" onclick=`test(${chunk.slug})`)= chunk.slug
        else
            li There no chunks

    button(onclick=`saveChunks()`) Save Chunks
