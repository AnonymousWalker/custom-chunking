extends index.pug

block append scripts
    script(type='module', src='/js/chunking_component.js')

block action
    +action Create Chunks

block breadcrumbs
    -
        var crumbs = [
            {name: "HOME", href: "/"},
            {name: params.lang.toUpperCase(), href: `/${params.lang}`},
            {name: params.res.toUpperCase(), href: `/${params.lang}/${params.res}`},
            {name: params.book.toUpperCase(), href: `/${params.lang}/${params.res}/${params.book}`}
        ]
    +breadcrumbs(crumbs, params.chapter.toUpperCase())
block content
    div(id='chunkingComponent' data-usx=contents)
